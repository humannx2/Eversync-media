"use client";

export default function Portfolio() {
  return (
    <section className="px-12 bg-white min-h-screen ">
            <div className="w-full">
                <span className="flex justify-between items-center border-b border-black">
                    <span className="text-sm font-[500]">{"Portfolio".toUpperCase()}</span>
                    <span>{"(02)"}</span>
                </span>
            </div>
    </section>
  )
}
